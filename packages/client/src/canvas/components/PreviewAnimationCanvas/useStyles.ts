import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  wrapper: {
    position: 'relative',
    display: 'flex',
    justifyContent: 'center',
  },
  mainCanvas: {
    position: 'absolute',
  },
}));
