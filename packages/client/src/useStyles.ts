import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  version: {
    position: 'fixed',
    margin: 'auto 0 10px 10px',
    bottom: 0,
    left: 0,
    color: 'black',
    opacity: 0.5,
    fontSize: '12px',
  },
}));
